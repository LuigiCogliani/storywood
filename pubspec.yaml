name: irina_storywood_mockup
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1002.6.3+16

environment:
  sdk: ">=2.19.0 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.2 # keep ^0.13.5

  provider: ^6.0.5
  books_finder: ^4.3.0
  podcast_search: ^0.6.3 # keep ^0.5.4
  shared_preferences: ^2.2.0
  firebase_core: ^2.15.1
  cloud_firestore: ^4.9.1
  youtube_player_flutter: ^8.1.2
  firebase_messaging: ^14.6.7
  overlay_support: ^2.1.0
  firebase_auth: ^4.9.0
  get: ^4.6.5
  intl: ^0.18.1 # keep ^0.17.0
  url_launcher: ^6.1.10
  readmore: ^2.2.0
  flutter_riverpod: ^2.3.6
  email_validator: ^2.1.17
  pull_down_button: ^0.9.1
  package_info_plus: ^4.2.0 # keep ^3.0.2
  badges: ^3.1.1
  firebase_crashlytics: ^3.4.4
  # app_version: ^0.0.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  # The following adds the multiselect package
  # https://www.flutterdecode.com/multi-select-dropdown/
  multiselect: ^0.1.0
  auto_size_text: ^3.0.0
  firebase_storage: ^11.2.6
  image_picker: ^1.0.4
  flutter_app_badger: ^1.5.0
  smooth_page_indicator: ^1.1.0
  #TODO add to the privacy policy document
  carousel_slider: ^4.2.1
  # firebase_ui_firestore is used for future builder with pagination
  firebase_ui_firestore: ^1.5.14 # keep 1.4.0
  firebase_analytics: ^10.4.5
  new_version_plus: ^0.0.6
  upgrader: ^9.0.0
  flutter_inappwebview: ^5.8.0
  tutorial_coach_mark: ^1.2.11 #package for in-app tutorials
  font_awesome_flutter: ^10.6.0 #package to use font awesome icons for favourite genres

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.1
  flutter_launcher_icons: ^0.13.1 # keep ^0.11.0

flutter_icons:
  android: true
  ios: true
  image_path: "dev_assets/places.png"
  adaptive_icon_background: "#191919"
  adaptive_icon_foreground: "dev_assets/places-adaptive.png"
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/tmdb_logo.png
    - assets/images/android_1.png
    - assets/images/android_2.png
    - assets/images/android_3.png
    - assets/images/android_4.png
    - assets/images/android_5.png
    - assets/images/android_6.png
    - assets/images/ios_1.png
    - assets/images/ios_2.png
    - assets/images/ios_3.png
    - assets/images/ios_4.png
    - assets/images/ios_5.png
    - assets/images/ios_6.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: NotoSerif
      fonts:
        - asset: assets/fonts/NotoSerif-Regular.ttf
        - asset: assets/fonts/NotoSerif-Bold.ttf
          weight: 700
        - asset: assets/fonts/NotoSerif-Italic.ttf
          style: italic

    - family: Inika
      fonts:
        - asset: assets/fonts/Inika-Regular.ttf
        - asset: assets/fonts/Inika-Bold.ttf
          weight: 700
    - family: ios
      fonts:
        - asset: assets/fonts/SF-Pro.ttf
    - family: Oregano
      fonts:
        - asset: assets/fonts/Oregano-Regular.ttf
    - family: AbrilFatface
      fonts:
        - asset: assets/fonts/AbrilFatface-Regular.ttf
    - family: FrederickatheGreat
      fonts:
        - asset: assets/fonts/FrederickatheGreat-Regular.ttf
    - family: PatuaOne
      fonts:
        - asset: assets/fonts/PatuaOne-Regular.ttf
    - family: TiltNeon
      fonts:
        - asset: assets/fonts/TiltNeon-Regular.ttf

    - family: IrishGrover
      fonts:
        - asset: assets/fonts/IrishGrover-Regular.ttf

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
